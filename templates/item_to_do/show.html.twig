{% extends 'base.html.twig' %}

{% block title %}ItemToDo{% endblock %}

{% block body %}
    <h1>ItemToDo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ item_to_do.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ item_to_do.name }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ item_to_do.content }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ item_to_do.createdAt ? item_to_do.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('item_to_do_index') }}">back to list</a>

    <a href="{{ path('item_to_do_edit', {'id': item_to_do.id}) }}">edit</a>

    {{ include('item_to_do/_delete_form.html.twig') }}
{% endblock %}
